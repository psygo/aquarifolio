import { ExtrudedSvg } from "../ExtrudedSvg";

export function NextjsLogo() {
  return (
    <ExtrudedSvg
      filename="next.js.svg"
      scale={0.005}
      depth={20}
      position={[0, 21, 0]}
    />
  );
}
