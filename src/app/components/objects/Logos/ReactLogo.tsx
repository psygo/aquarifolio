import { ExtrudedSvg } from "../ExtrudedSvg";

export function ReactLogo() {
  return (
    <ExtrudedSvg
      filename="react.svg"
      scale={0.005}
      depth={50}
      position={[1, 17, 0]}
    />
  );
}
